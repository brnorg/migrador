{
  "name": "{{ project_name | slugify }}",
  "version": "0.1.0",
  "private": true,
  "description": "Projeto gerado por repo-template-cli",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": "{{ runtime_version }}"
  }
}
